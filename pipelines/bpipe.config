// Set memory requirements for each stage to allow bpipe to determine which can run concurrently

// Default
memory="1g"

commands {

    mosdepth {
        memory="2g"
    }

    bwamem {
        memory="26g"
    }

    merge {
        memory="4g"
    }

    bedtools {
        memory="2g"
    }

    locuscounts {
        memory="13g"
    }
  
}
